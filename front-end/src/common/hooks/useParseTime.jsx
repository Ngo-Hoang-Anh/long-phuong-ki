import React from 'react'
import Proptypes from 'prop-types';
const useParseTime = ({ times }) => {
    return (
        <div>useParseTime</div>
    )
}

useParseTime.propTypes = {
    times: Proptypes.object,
}
export default useParseTime